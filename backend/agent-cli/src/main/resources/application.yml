spring:
  application:
    name: agent-cli
  main:
    banner-mode: off
    web-application-type: none

logging:
  level:
    root: WARN
    com.aiagent: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
